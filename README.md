The Simplest Graph Renderer there is

Dependencies:

    * GLEW
    * OpenGL
    * GLFW

To build:
    
    mkdir build
    cd build
    cmake ../

To run:

    ./simple -gf ../../fmigraph/saarland.gl
